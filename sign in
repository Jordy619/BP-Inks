<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>BP Inks Co - Log In</title>
    <style>
        /* Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;	
        }

        /* Header styles */
        .header {
            background-color: #3aa314;
            color: white;
            padding: 10px;
            text-align: center;
            font-size: 14px;
        }

        .header a {
            color: white;
            text-decoration: none;
            margin: 0 10px;
        }

        /* Logo bar */
        .logo-bar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px;
            border-bottom: 1px solid #ddd;
			 background-color: #AFAFAF;
        }

        .logo-bar h1 {
            color: #FF4500;
            font-size: 24px;
            margin: 0;
        }

        .logo-bar h1 span {
            color: black;
        }

        .logo-bar .nav-links a {
            color: black;
            text-decoration: none;
            margin: 0 15px;
            font-size: 16px;
        }

        /* Main login container */
        .login-container {
            max-width: 500px;
            margin: 50px auto;
            padding: 20px;
            text-align: center;
        }

        .login-container h2 {
            font-size: 32px;
            margin-bottom: 10px;
        }

        .login-container p {
            font-size: 16px;
            margin-bottom: 20px;
            color: #555;
        }

        /* Login form */
        .login-form input[type="email"] {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            border: 1px solid #ddd;
            border-radius: 4px;
            font-size: 16px;
        }

        .login-form button {
            width: 100%;
            padding: 12px;
            margin: 10px 0;
            background-color: #0056d2;
            color: white;
            border: none;
            border-radius: 4px;
            font-size: 16px;
            cursor: pointer;
        }

        .login-form button.google-login {
            background-color: white;
            color: #555;
            border: 1px solid #ddd;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .login-form button.google-login img {
            width: 18px;
            height: 18px;
            margin-right: 10px;
        }

        /* OR divider */
        .or-divider {
            display: flex;
            align-items: center;
            text-align: center;
            color: #888;
            margin: 20px 0;
        }

        .or-divider::before,
        .or-divider::after {
            content: "";
            flex: 1;
            border-bottom: 1px solid #ddd;
        }

        .or-divider:not(:empty)::before {
            margin-right: .5em;
        }

        .or-divider:not(:empty)::after {
            margin-left: .5em;
        }

        /* Footer text */
        .footer-text {
            margin-top: 20px;
            font-size: 14px;
        }

        .footer-text a {
            color: #FF4500;
            text-decoration: none;
        }
    </style>
</head>
<body>

    <!-- Header notification bar -->
    <div class="header">
        Custom T-shirts & Promotional Products, Fast & Free Shipping, and All-Inclusive Pricing
    </div>
	
    <!-- Logo and Navigation -->
    <div class="logo-bar">
        <h1><a href="Homepage.html" class="logo-link">BP Inks <span>Co</span></a></h1>
        <div class="nav-links">
            <a href="#">Products</a>
            <a href="#">Design Lab</a>
            <a href="#">Templates</a>
            <a href="#">Solutions</a>
        </div>
    </div>
